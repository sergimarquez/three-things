[build]
  command = "npm run test:run && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect (backup for _redirects file)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
